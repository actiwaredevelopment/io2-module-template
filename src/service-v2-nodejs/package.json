{
    "name": "io2-module-iotemplate",
    "version": "2.0.0",
    "description": "",
    "main": "../../build-service/nodejs/index.js",
    "dependencies": {
        "express": "^4.17.1"
    },
    "devDependencies": {
        "@types/express": "^4.17.13",
        "@types/node": "^16.9.0",
        "nodemon": "^2.0.12",
        "ts-node": "^10.2.1",
        "typescript": "^4.4.2"
    },
    "scripts": {
        "start": "export NODE_ENV=development && nodemon index.ts",
        "build": "tsc --build && tsc-alias -p tsconfig.json",
        "build:watch": "tsc -w & tsc-alias -w"
    }
}